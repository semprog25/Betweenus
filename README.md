
  # Between Us

  This is a code bundle for Between Us. The original project is available at https://www.figma.com/design/Htv3MgmBtwpToysNxDHP4m/Between-Us.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  